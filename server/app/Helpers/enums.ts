export enum TicketStatus {
  OPEN = 'Open',
  CLOSED = 'Closed',
  RESPONDED = 'Responded',
}

export enum ImageTypes {
  IMG = 'Image',
  THUMB = 'Thumbnail',
  AVATAR = 'Avatar',
  COVER = 'Cover',
  BROCHER = 'Brocher',
}

export enum VideoType {
  VIDEO = 'Video',
}

export enum ServiceLocationType {
  SPECIFIC = 'Specific',
  PHYSICAL = 'Physical',
}
